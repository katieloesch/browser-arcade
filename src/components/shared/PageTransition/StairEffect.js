import React from 'react';
import { AnimatePresence, motion } from 'framer-motion';

import Stairs from './Stairs';
import './StairEffect.scss';

const StairEffect = () => {
  const game = window.location.pathname.substring(1);

  return <></>;
};

export default StairEffect;
